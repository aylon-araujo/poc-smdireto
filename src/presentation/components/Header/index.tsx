import { FC } from 'react';

import HeaderPage from './Header';

const Appbar: FC<JSX.Element> = () => {
  return <HeaderPage key={null} type={undefined} props={undefined} />;
};

export default Appbar;
